default: []

build:
    - 'clean:build'
    - 'tslint'
    - 'ts:build'
    - 'clean:basedir'

build_nosourcemaps:
    - 'build'
    - 'clean:maps'
    
test:
    - 'clean:test'
    - 'tslint'
    - 'ts:test'
    - 'clean:basedir'
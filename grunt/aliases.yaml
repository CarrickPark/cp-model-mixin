default: [ ]

pre_build:
    - 'clean:build'
    - 'tslint'

post_build:
    - 'clean:basedir'

build:
    - 'pre_build'
    - 'ts:build'
    - 'uglify'
    - 'post_build'
    
build_debug:
    - 'pre_build'
    - 'ts:build_debug'
    - 'post_build'

build_debug_js:
    - 'clean:maps'
    - 'ts:build'
    - 'post_build'

test:
    - 'build'
    - 'mochaTest'

cover:
    - 'build'
    - 'mocha_istanbul'